{
  "F1": {
    "title": "Immutable metadata hash integrity",
    "scope": "Manifest metadata hashes must bind the exact persisted DocumentVersion title and document URI; explicit correction retains historical lineage and auditable idempotency.",
    "recordedOutcome": "Executed at current HEAD: 9 passed in the public snapshot. Combined run: 20 passed, zero failures/skips, 54.84s. Record: architecture-explorer/verification/current-head-regressions.json; local-HEAD results preserved separately.",
    "environment": "Existing graph fixture creates a UUID kos-v01-regression database for each graph test, migrates only that database, uses temporary SQLite, then drops and verifies removal of only that UUID database. Python 3.12.11 / pytest 8.4.2 on Darwin; PYTHONPATH=src.",
    "limitations": "Only the declared finding is reverified. Public snapshot excludes separate local temporal remediation. Both execution revisions remain recorded. F3–F8 and release acceptance remain open.",
    "resolutionRequired": ""
  },
  "F2": {
    "title": "Deterministic deletion replay",
    "scope": "Reconstructing a deletion after a graph commit but before local checkpoint persistence must reproduce the exact manifest and not duplicate history; later independent deletions remain distinct.",
    "recordedOutcome": "Executed at current HEAD: 11 passed in the public snapshot. Combined run: 20 passed, zero failures/skips, 54.84s. Record: architecture-explorer/verification/current-head-regressions.json; local-HEAD results preserved separately.",
    "environment": "Existing graph fixture creates a UUID kos-v01-regression database for each graph test, migrates only that database, uses temporary SQLite, then drops and verifies removal of only that UUID database. Python 3.12.11 / pytest 8.4.2 on Darwin; PYTHONPATH=src.",
    "limitations": "Only the declared finding is reverified. Public snapshot excludes separate local temporal remediation. Both execution revisions remain recorded. F3–F8 and release acceptance remain open.",
    "resolutionRequired": ""
  },
  "F3": {
    "title": "Doctor integrity coverage",
    "scope": "Doctor must accept the current governance contract and detect corrupt metadata, missing constraints/indexes, and multi-node supersession cycles.",
    "recordedOutcome": "Open according to the current remediation workflow; no subsequent accepted resolution is recorded here.",
    "environment": "",
    "limitations": "The metadata-hash subcase overlaps the F1 correction. That does not establish that the complete F3 finding is resolved.",
    "resolutionRequired": "Execute and reconcile all F3 cases against isolated real Neo4j, then record scoped acceptance."
  },
  "F4": {
    "title": "Policy reviewer enforcement",
    "scope": "The Notion executor must reject any reviewer other than the exact policy reviewer before acquiring a claim or contacting the provider.",
    "recordedOutcome": "Open according to the current remediation workflow; no subsequent accepted resolution is recorded here.",
    "environment": "",
    "limitations": "These executable regressions define unresolved remediation scope. Their current outcome was not rerun by the Explorer.",
    "resolutionRequired": "Demonstrate zero claims and zero provider calls for unauthorized reviewers; run affected executor tests and record remediation acceptance."
  },
  "F5": {
    "title": "Scalar predicate validation",
    "scope": "Scalar-valued Assertion changes currently return before relation validation; an unregistered predicate must be rejected.",
    "recordedOutcome": "Open according to the current remediation workflow; no subsequent accepted resolution is recorded here.",
    "environment": "",
    "limitations": "These executable regressions define unresolved remediation scope. Their current outcome was not rerun by the Explorer.",
    "resolutionRequired": "Prove rejection for scalar values, preserve valid typed relations, and record the corrected invariant."
  },
  "F6": {
    "title": "Hard chunk bounds",
    "scope": "Long unbroken or oversized paragraphs must honor chunk_size without losing evidence text.",
    "recordedOutcome": "Open according to the current remediation workflow; no subsequent accepted resolution is recorded here.",
    "environment": "",
    "limitations": "These executable regressions define unresolved remediation scope. Their current outcome was not rerun by the Explorer.",
    "resolutionRequired": "Pass long ASCII, Korean and mixed-paragraph regressions without truncating source content and reconcile processing identity implications."
  },
  "F7": {
    "title": "Authorization evaluation exclusions",
    "scope": "Denied-result checks must inspect raw retrieval output before ignored-document exclusions can hide a leak.",
    "recordedOutcome": "Open according to the current remediation workflow; no subsequent accepted resolution is recorded here.",
    "environment": "",
    "limitations": "These executable regressions define unresolved remediation scope. Their current outcome was not rerun by the Explorer.",
    "resolutionRequired": "Prove a leaked raw result fails the denial gate despite exclusions; run affected evaluation tests and record acceptance."
  },
  "F8": {
    "title": "Graph / audit failure recovery",
    "scope": "A committed graph mutation must not permanently lose its SQLite audit evidence or be reported as an unqualified failure when the operational store fails.",
    "recordedOutcome": "Open according to the current remediation workflow; no subsequent accepted resolution is recorded here.",
    "environment": "",
    "limitations": "These executable regressions define unresolved remediation scope. Their current outcome was not rerun by the Explorer.",
    "resolutionRequired": "Use real Neo4j plus an injected operational-store outage to prove truthful commit outcomes and exactly one reconciled audit record on retry."
  },
  "release-gate": {
    "title": "v0.1 release acceptance",
    "scope": "v0.1 remains under correctness verification. Earlier operational evidence is not a completed current release gate.",
    "recordedOutcome": "Open according to the current remediation workflow; no subsequent accepted resolution is recorded here.",
    "environment": "",
    "limitations": "These executable regressions define unresolved remediation scope. Their current outcome was not rerun by the Explorer.",
    "resolutionRequired": "Resolve remaining findings through regression-driven remediation and explicitly execute the full release acceptance gate."
  }
}
