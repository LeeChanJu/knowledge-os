Split the note so a precise passage can be cited. The split must bound size without losing source text; the hard-bound issue for long paragraphs remains open.

<!-- DEPTH -->

### Why this responsibility exists

Give claims stable, inspectable support and expose evidence only under current source authorization.

### Inputs and outputs

Input: Version identity, chunk order, text, processing metadata.

Output: Authorized exact evidence and retrieval candidates.

### What must be preserved

- Evidence text, version link, lifecycle flags, optional embedding provenance.
- Current ACL and active Document lifecycle govern reads. Historical Chunk provenance is retained even when its source is deleted.

### Follow the example

Cite the passage saying “review stopped” rather than the whole note. Even identical text needs the identity of its document version and passage.

### Avoid this misconception

Current ACL and active Document lifecycle govern reads. Historical Chunk provenance is retained even when its source is deleted.
