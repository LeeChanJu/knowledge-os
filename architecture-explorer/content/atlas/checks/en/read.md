### How does an AI answer differ from what Knowledge OS returns?

Knowledge OS returns evidence and relations through bounded reads. The external AI writes the answer; check whether the returned material actually supports its explanation.

### Does an old evidence snapshot preserve access after revocation?

A historical ACL is provenance, not a permanent grant. Current document authorization and active state govern access.
